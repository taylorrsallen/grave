class_name EquippableBase extends InteractableBase

# (({[%%%(({[=======================================================================================================================]}))%%%]}))
func equip(character: Character) -> void:
	pass
